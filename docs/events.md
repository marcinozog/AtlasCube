# Events — reminders and notifications

Reminder system: one-shot or recurring events (birthdays, namedays,
anniversaries, plain reminders) stored as JSON, edited via the web UI,
fired at a scheduled time by a background scheduler. Notification =
buzzer pattern + a fullscreen LVGL screen with an encoder "dismiss"
button. Plus a small indicator on selected screens showing how many
events are still pending today.

## Components involved

| Component | Role |
|---|---|
| [components/events/](../components/events/) | Domain: model, CRUD, scheduler, JSON I/O |
| [components/buzzer/](../components/buzzer/) | LEDC tone patterns on `BUZZER_GPIO` |
| [components/web/](../components/web/) | REST endpoints `/api/events*` |
| [components/ui/screens/screen_event_notification.c](../components/ui/screens/screen_event_notification.c) | Fullscreen toast screen |
| [components/ui/widgets/event_indicator_widget.c](../components/ui/widgets/event_indicator_widget.c) | 🔔 corner icon on other screens |
| [spiffs_image/www/events.html](../spiffs_image/www/events.html) + [events.js](../spiffs_image/www/events.js) | CRUD page |
| `/spiffs/events.json` | SPIFFS persistence |

## Data model

`event_t` defined in [components/events/events_service.h](../components/events/events_service.h):

| Field | Type | Range / description |
|---|---|---|
| `id` | `char[9]` | 8 hex chars + `\0`, generated by `esp_random()` on add |
| `type` | enum | `EV_BIRTHDAY` / `EV_NAMEDAY` / `EV_REMINDER` / `EV_ANNIVERSARY` |
| `title` | `char[64]` | Text shown on screen and in web list |
| `year`, `month`, `day` | int | Base date. For `yearly` the year is irrelevant (commemorative). |
| `hour`, `minute` | int | Trigger time (local TZ) |
| `recurrence` | enum | `none` / `daily` / `weekly` / `monthly` / `yearly` |
| `enabled` | bool | `false` excludes the event from firing and from the 🔔 counter |

Maximum `EVENTS_MAX = 50` events. Static array in RAM, guarded by a mutex
against concurrent access from scheduler and CRUD.

Plus runtime-only state (RAM, not persisted):

- `s_fired_today[EVENTS_MAX]` — "already fired today" flag, prevents
  double-firing. Reset on day rollover and on `events_add()` /
  `events_update()`.
- `s_last_day` — month-day from the previous tick, used to detect day
  rollover.

## Persistence — `/spiffs/events.json`

Format:

```json
{
  "events": [
    {
      "id": "a3f19c2b",
      "type": "reminder",
      "title": "Pills",
      "year": 2026, "month": 4, "day": 23,
      "hour": 8, "minute": 0,
      "recurrence": "daily",
      "enabled": true
    }
  ]
}
```

Atomic write in `save_to_file()`: write to `events.json.tmp`, then
`remove(events.json)` + `rename(.tmp → .json)`. ESP reset mid-write
won't damage the previous file. SPIFFS requires removing the
destination before rename — hence the two-step.

File loaded on `events_service_init()`. If missing or unparseable, we
start with an empty list and write a fresh one.

## Recurrence rules — when does an event match today

`matches_today()` in [events_service.c](../components/events/events_service.c):

| Mode | Condition |
|---|---|
| `none` | Exact `year + month + day` match |
| `daily` | Always true |
| `weekly` | `weekday(base_date) == weekday(today)` — computed via `mktime()` + `localtime_r()` |
| `monthly` | `tm_mday == e->day` |
| `yearly` | `tm_mon+1 == e->month && tm_mday == e->day` (base year ignored) |

Intentionally not handled:

- **February 29** in `yearly` — in non-leap years `matches_today` returns
  false, the event simply doesn't fire that year.
- **Day > 28 in `monthly`** — in shorter months `matches_today` returns
  false, no rollover to the last day of the month.

## Scheduler

In [events_service.c](../components/events/events_service.c), started
from `events_service_init()`:

- Periodic `esp_timer`, interval `EVENTS_TICK_US = 30 * 1000 * 1000` (30 s).
- Calls `check_events()` plus an immediate first check (so we don't wait
  30 s after boot).

`check_events()` flow:

1. If `ntp_service_is_synced() == false` → bail. Without time we don't
   fire anything.
2. `time(NULL)` → `localtime_r(&now, &lt)`. Timezone from `TZ` env —
   set by [ntp_service](../components/services/ntp_service.c) at init.
3. Day rollover detection: if `s_last_day != lt.tm_mday` → zero
   `s_fired_today[]`, store new day. Handles e.g. "event on April 20,
   fired before midnight, at 00:00:30 the next day we re-check from
   scratch".
4. Compute `now_min = lt.tm_hour * 60 + lt.tm_min`.
5. For each event: if `matches_today(e, &lt)` is true, compute
   `target = e->hour * 60 + e->minute`. If the **1-minute window**
   `now_min ∈ [target, target+1]` and `!fired_today` → `fire_event(e)` +
   `fired_today = true`.

The 1-minute window with a 30 s tick guarantees every event is caught —
between any two ticks one will land inside the window.

**"Missed event" case**: ESP boots at 10:30, event scheduled for 08:00
the same day. Scheduler checks today's events — `target=480 < now_min=630`,
window already past, `fired_today` not set (but nothing fires either).
Next day flags reset, the event will fire again per recurrence rules.

## Triggering — `fire_event()`

Two actions:

**1. Buzzer.** Pattern depends on type:
- `birthday` / `anniversary` → `MELODY_BDAY` ("Happy Birthday" snippet)
- `reminder` / `nameday` → `BEEP_REMINDER` (3× short 880 Hz tone)

Patterns in [events_service.c](../components/events/events_service.c),
played by `buzzer_beep_pattern()` (FreeRTOS queue, task playing
`freq_hz, duration_ms` pairs).

**2. UI callback.** Invokes pointer `s_fire_cb` (if registered).
Dependency inversion: `events_service` doesn't know about UI, the UI
registers itself via `events_service_set_fire_cb()`. Translation to
`UI_EVT_EVENT_FIRED` happens in
[components/ui/ui_manager.c](../components/ui/ui_manager.c) function
`on_event_fired()` — that's where a `ui_event_t` with
`id/title/type_label/hour/minute` is built and pushed onto the UI queue.

## Notification screen

[components/ui/screens/screen_event_notification.c](../components/ui/screens/screen_event_notification.c)
— full screen implementing `ui_screen_t`. 320×240 layout:

- Big 🔔 symbol (LV_SYMBOL_BELL) in 96 px font
- Type label (e.g. "REMINDER") in 14 px font
- Event title in 18 px (scrolls when long)
- HH:MM time in 14 px accent color
- Bottom hint "✓ press encoder"

Transition to the screen: when `UI_EVT_EVENT_FIRED` arrives, the
`ui_manager_run()` dispatcher:

1. Stores `s_active_id` as the return screen (unless we're already on
   event_notification — then keep the previous return target so re-fires
   preserve the original origin).
2. `screen_event_notification_set_info()` + `set_return()` — passes
   event data and return screen via module-static variables.
3. `do_navigate(SCREEN_EVENT_NOTIFICATION)` — destroys current screen,
   creates event_notification.

Dismiss: `on_input()` reacts to `UI_INPUT_ENCODER_PRESS` and
`UI_INPUT_BTN_OK` → `ui_navigate(s_return_to)` — rebuilds the previous
screen.

## Corner indicator — event_indicator_widget

[components/ui/widgets/event_indicator_widget.c](../components/ui/widgets/event_indicator_widget.c)
— small label placed manually on selected screens (e.g.
[screen_clock.c](../components/ui/screens/screen_clock.c)) next to
`mode_indicator_widget`.

Display:
- `events_pending_today_count() == 0` → hidden
- `== 1` → just `🔔`
- `>= 2` → `🔔 N`

Refreshes **on its own** via an internal `lv_timer` every 30 s.
Decision: we don't rely on a global `UI_EVT_CLOCK_TICK` (the enum exists
but nobody emits it in this project), nor on callbacks from
`events_service`. With a 30 s interval the counter shows up within 30 s
of adding an event from the web UI — acceptable.

Counter function in [events_service.c](../components/events/events_service.c):

```c
int events_pending_today_count(void);
```

Counts only events satisfying all of:
- `enabled == true`
- `!s_fired_today[i]`
- `matches_today(e, &lt)` true
- `target = hour*60 + minute >= now_min` (still ahead today)

So for an anniversary on April 20: the bell appears only between
midnight and the trigger time **on that specific day**. Outside April 20
— `matches_today` returns false, no bell.

## REST API

In [components/web/http_server.c](../components/web/http_server.c):

| Method | Path | Body / effect |
|---|---|---|
| `GET` | `/api/events` | Array of all events (JSON) |
| `POST` | `/api/events` | Body: event JSON (id optional). Returns the created object. |
| `PUT` | `/api/events/{id}` | Body: partial or full event JSON (partial patch). |
| `DELETE` | `/api/events/{id}` | Deletes the event |

Validation in `event_validate()` — non-empty `title`, field ranges, type
in allowed enum. Bad fields → HTTP 400 with the error text.

`PUT` with a single field works as a "patch" — `event_patch_from_json()`
overwrites only fields present in body. Used by the "Enabled" toggle in
the list: `fetch('/api/events/xyz', { method: 'PUT', body: '{"enabled":false}' })`.

## Web page

[spiffs_image/www/events.html](../spiffs_image/www/events.html) — single
page: form on top (add / edit) + event list below. JavaScript in
[events.js](../spiffs_image/www/events.js):

- `evLoad()` — fetch GET `/api/events`, render list
- `evSave()` — POST or PUT depending on `editingId`
- `evEdit(id)` — fills the form, switches to edit mode
- `evDelete(id)` — `confirm()` + DELETE
- `evToggleEnabled(id, on)` — PUT with one field

On form reset the date defaults to today and time to now (helpers
`todayStr()` / `nowTimeStr()`) — fewer clicks for the common "add for
now" case.

## Startup integration

In `app_main` (or `settings_apply` / equivalent):

```c
buzzer_init(BUZZER_GPIO);     // LEDC timer 1 / channel 1 (timer 0 taken by LCD backlight)
ui_manager_init();            // registers events → UI callback
// ... NTP, WiFi, HTTP ...
events_service_init();        // loads JSON, starts scheduler
```

Order is flexible — the scheduler waits for NTP sync on its own,
`set_fire_cb` can be called any time (static pointer in events_service).

## Known limitations

- `fired_today` flags live in RAM. ESP reset before midnight → an event
  fired earlier the same day will fire again after boot if it lands in
  the time window. Tradeoff — alternative is persisting the flags to
  SPIFFS on every fire, which we don't want.
- Timezone. Changing TZ at runtime via `ntp_service_reconfigure()` does
  **not** call `events_service_on_tz_changed()` automatically — no hook.
  The hook is prepared in the API but intentionally not wired to avoid
  a circular dependency. In practice: set TZ once on first run.
  `settings.html` has a warning about this.
- Buzzer patterns are fixed — not configurable per event.
- Maximum 50 events.
