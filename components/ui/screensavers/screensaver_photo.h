#pragma once

#include "ui_screen.h"

// Photo-frame screensaver: streams pre-converted LVGL RGB565 .bin slides from
// the SD card and reveals each one slowly as a transition effect.
extern const ui_screen_t screensaver_photo;
